import React from 'react';

// Components
import Template from '../components/Template';

const PlayerScreen = () => (
    <Template>
        PlayerScreen
    </Template>
);

export default PlayerScreen;
